# Based on: https://www.toptal.com/developers/gitignore/api/hugo

### Hugo ###

# Don't track source files (since we're pushing the whole generated site for GitHub Pages anyway)
content/
static/

# Generated files by hugo (not ignoring docs/ so that it's used by GitHub Pages to publish the site)
/resources/_gen/
/assets/jsconfig.json
hugo_stats.json

# Executable may be added to repository
hugo.exe
hugo.darwin
hugo.linux

# Temporary lock file while building
/.hugo_build.lock

### Local files ###
_local/